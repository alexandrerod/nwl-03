import { createConnection } from 'typeorm';

//só chamar essa função e a conexão com o banco de dados
//já está estabelecida
//chamar esse arquivo dentro do server para ser executado.
createConnection();